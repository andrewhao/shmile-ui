var ShmileStateMachine = require('../../ui/shmileStateMachine'),
    StateMachine = require('javascript-state-machine')

describe('ShmileStateMachine', function() {
	beforeEach(function() {
		this.subject = new ShmileStateMachine()
	});
});
